@using front_to_back.Areas.Admin.ViewModels
@model RecentWorkIndexViewModel
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Recent Work Component</h4>
                        <a class="btn btn-main" href="/admin/recentwork/create">Create work</a>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>Text</th>
                                        <th>FilePath</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var component in Model.RecentWorkComponents)
                                    {
                                        <tr>
                                            <td>@component.Id</td>
                                            <td>@component.Title</td>
                                            <td>@component.Text</td>
                                            <td>@component.FilePath</td>
                                            <td  >
                                                <a class="btn btn-primary" asp-action="update" asp-controller="recentwork" asp-area="admin" asp-route-id="@component.Id">
                                                Update
                                            </a>
                                            
                                            <a class="btn btn-danger" asp-action="delete" asp-controller="recentwork" asp-area="admin" asp-route-id="@component.Id">
                                                delete
                                            </a>
                                            <a class="btn btn-danger" asp-action="details" asp-controller="recentwork" asp-area="admin" asp-route-id="@component.Id">
                                                details
                                            </a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>